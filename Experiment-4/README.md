# Exp-4

React-based experiments for state management.

## Structure

- 4.1/ - Minimal React Context API app (theme toggle and objective).
- 4.2/ - Redux Toolkit counter demo (counter-only UI).

## Usage

For each React app, install dependencies and run the dev server:

- `npm install`
- `npm run dev`
